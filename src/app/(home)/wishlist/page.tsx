import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Wishlist | Qatoto",
  description: "Wishlist page for Qatoto",
};

export default function Wishlist() {
  return <h1>Wishlist</h1>;
}
