import ShowcaseFeedSkeleton from "@/components/home/blueprints/showcase/showcase-feed-skeleton";

/** Feed-shaped, unlike the hub-shaped `blueprints/loading.tsx` this segment used to inherit. */
export default function Loading() {
  return <ShowcaseFeedSkeleton />;
}
